apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: myapp          # แก้ namespace ให้ตรงกับ Deployment
  labels:
    app: backend
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
